You can run tests by running the command:
`python -m pytest -vv --html=report.html --self-contained-html -n auto`

Running tests with the above command has a few additional required libraries:
* `pytest`
* `pytest-html`
* `pytest-xdist`
